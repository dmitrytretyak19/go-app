# ФАЙЛ: prometheus.yml
# НАЗНАЧЕНИЕ: Конфигурация Prometheus для сбора метрик

global:
  scrape_interval: 15s  # Собирать метрики каждые 15 секунд

scrape_configs:
  - job_name: 'go-app'
    static_configs:
      - targets: ['host.docker.internal:8080']  # Твой локальный сервер